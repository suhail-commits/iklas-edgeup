apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "edgeup-chart.fullname" . }}-test-connection"
  labels:
    {{- include "edgeup-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "edgeup-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
